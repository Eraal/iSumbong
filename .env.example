# Copy this file to .env and fill in the values

# Application
APP_URL=https://isumbong.live

# Database
DB_HOST=localhost
DB_PORT=3306
DB_NAME=isumbong
DB_USER=isumbong_user
DB_PASSWORD=change-me-strong

# SMTP (Gmail App Password recommended)
# Local and production can use the same Gmail SMTP host/port, but credentials and APP_URL differ.
# You can override HOST/PORT/ENCRYPTION if using another provider (e.g., Mailgun, SendGrid).
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_ENCRYPTION=tls
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-gmail-app-password
# Optional overrides (defaults to SMTP_USERNAME if not set)
# FROM_EMAIL=
# FROM_NAME=iSUMBONG System
# REPLY_TO_EMAIL=

# Tesseract OCR
# If Tesseract isn't in PATH on your server, set the full path here.
# Linux example:
# TESSERACT_PATH=/usr/bin/tesseract
# Windows example:
# TESSERACT_PATH=C:\\Program Files\\Tesseract-OCR\\tesseract.exe
